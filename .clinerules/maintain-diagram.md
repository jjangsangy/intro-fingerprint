# Maintain Architecture Diagram

- **File**: `memory-bank/systemPatterns.md`
- **Rule**: Whenever significant changes are made to the system architecture, data flow, or component interactions, you MUST update the Mermaid flowchart in the "Architecture Overview" section of `memory-bank/systemPatterns.md`.
- **Constraint**: Do NOT use HTML tags (like `<br/>`) or literal `\n` characters in node labels. Use text separators (e.g., `:`, `-`, `()`) or single-line labels to ensure compatibility with VS Code Markdown preview.
